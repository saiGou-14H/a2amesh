# A2AMesh 历史工作树与本地分支清理审计（2026-08-21）

> 审计 ID：`A2AM-WORKTREE-CLEANUP-20260821`
>
> 执行范围：根据用户授权，先移除 clean、空闲的历史 Git worktree 目录，再仅删除已能证明无用的本地 Git branch refs。
>
> 边界：未删除任何远端分支、未使用 `git gc`、未删除 Git 对象；含未提交修改、仍被进程使用、或无法证明可安全删除的项目均保留。

---

## 1. 清理前冻结

- Git 主仓：`/root/a2amesh`
- 清理前已注册 worktree：`54`
- 清理前本地 heads：`53`
- worktree 候选清单：`46` 个目录，全部满足：
  - `git status --porcelain` 为空；
  - 无进程以该目录或其子目录为当前工作目录；
  - 不在当前开发、审计、排期、M9c、ADR-040、WP-00a 的保留清单中。
- 清理前 worktree manifest：`/tmp/a2amesh-worktree-cleanup-20260821T073450Z.json`
  - SHA-256：`c77ba823fbf103f461d629ea3d2b9c1dd0c91478dfa97e5e61f74e48af5f6985`
- 清理前 local-ref snapshot：`/tmp/a2amesh-branches-before-cleanup-20260821T073450Z.tsv`
  - SHA-256：`2d7c691bf9ee7e1ae99d674829a93e804ac53bcf027b0cac2391e933ad41dd3c`

## 2. 已执行的 worktree 目录清理

使用标准 `git worktree remove <path>` 逐个移除 `46` 个 clean、空闲的历史工作目录；没有使用 `--force`。

按工作包归类，已移除目录包括：

- C0：`a2amesh-c0-1`、`a2amesh-c0-2`、`a2amesh-c0-2b`、`a2amesh-c0-2c`、`a2amesh-c0-review-fix`；
- C1：`a2amesh-c1-1`、其 remediation/fix worktrees、`a2amesh-c1-2`、`a2amesh-c1-3`、其 remediation worktrees、`a2amesh-c1-integration-1`～`3`；
- C2 历史 contract/key/client worktrees，以及 `a2amesh-c2-adr039-review`；
- 历史进度归档 worktree：`a2amesh-progress`。

唯一 detached 审阅 worktree 的 HEAD：

```text
0f585f09eeccc684ae8d2b64e3f1224360ffe82a
```

已在移除前验证为以下保留分支的祖先：排期、M9b、M9c、ADR-040、state-claim 及 WP-00a 等；因此不存在因该 detached worktree 移除而丢失唯一 commit 的情况。

## 3. 本地 branch ref 清理

本地 branch 审计只允许两类自动删除条件：

1. **`DELETE_MERGED`**：branch HEAD 是至少一个保留/活跃分支的祖先；
2. **`DELETE_REMOTE_MIRROR`**：`origin` 上存在同名且完全相同 SHA 的 branch。

删除前针对每个 ref 再次核验：branch 未被 surviving worktree checkout、SHA 未变化、上述条件仍成立；然后用带预期旧 SHA 的 `git update-ref -d` 删除本地 ref。

结果：

| 类别 | 数量 | 结果 |
|---|---:|---|
| `DELETE_MERGED` | 39 | 已删除本地 ref；提交仍由保留分支包含。 |
| `DELETE_REMOTE_MIRROR` | 1 | 已删除本地 `work/c2-s-lease-contract`；`origin/work/c2-s-lease-contract` 在审计时仍是同一 SHA。 |
| `RETAIN_UNPROVEN` | 5 | 保留，不以“历史”或“目录已移除”推断安全。 |

已删除的 merged refs：

```text
work/c0-ci-report
work/c0-ci-review-remediation
work/c0-wheel-fixtures
work/c1-1-remediation-10
work/c1-1-remediation-2
work/c1-1-remediation-3
work/c1-1-remediation-4
work/c1-1-remediation-5
work/c1-1-remediation-6
work/c1-1-remediation-7
work/c1-1-remediation-8
work/c1-1-remediation-9
work/c1-1-review-remediation
work/c1-3-remediation-2
work/c1-3-remediation-3
work/c1-3-remediation-4
work/c1-3-remediation-5
work/c1-3-remediation-6
work/c1-3-remediation-7
work/c1-context-dispatch
work/c1-integration-1
work/c1-integration-2
work/c1-integration-3
work/c1-operation-contracts
work/c1-task-state
work/c2-s-dispatch-contract
work/c2-s-lease-contract-fix1
work/c2-s-outbox-contract
work/c2-s-outbox-contract-fix1
work/c2-s-outbox-contract-fix2
work/c2-s-outbox-contract-fix3
work/c2-s-outbox-contract-fix4
work/c2-s-outbox-contract-fix5
work/c2-s-outbox-contract-fix6
work/c2-s-task-contract
work/c2-s-task-contract-fix1
work/c2-state-claim-contract-a0-docs
work/c2-state-key-builder
work/c2-state-redis-config-client-fix2
```

保留的未证明安全 refs：

| Branch | HEAD | 保留原因 |
|---|---|---|
| `work/c0-ci` | `f3c407dbdfe0` | 未证明已合入保留 branch，且无同 SHA remote mirror。 |
| `work/c0-official-fixtures` | `9d332c6a3323` | 同上。 |
| `work/c2-state-redis-config-client` | `f6d32a4e3836` | 同上。 |
| `work/c2-state-redis-config-client-fix1` | `9c823db1b6ce` | 同上。 |
| `work/progress-archive` | `bbf3164bceae` | 同上。 |

完整 ref 删除记录：`/tmp/a2amesh-local-branch-delete-20260821T074047Z.json`

- SHA-256：`7d77cbc90c0e63105353dd30760afb627ee06858cac2b24e28da0661f3a27aa1`

## 4. 明确保留、未触碰的项目

以下 worktree 未删除：

- `/root/a2amesh`（`main`）；
- `/root/a2amesh-c2-ci-nats-service`（M9b 审计基线）；
- `/root/a2amesh-c2-ci-nats-m9c`（含未提交 M9c WIP）；
- `/root/a2amesh-c2-claim-message-a1-adr`（含 ADR-040 未提交草案）；
- `/root/a2amesh-ci-hosted-diagnostic-wp00a`（含 WP-00a hardening WIP）；
- `/root/a2amesh-c2-state-claim-contract` 与 `/root/a2amesh-c2-state-claim-contract-p1-ci-schema`（当时被进程使用）；
- `/root/a2amesh-project-schedule-2026-08-21`（排期与执行日志）。

未删除 remote branches、未删除 untracked WIP、未删除审计基线和未批准 ADR，也未运行任何可能清除不可达对象的 Git garbage collection。

## 5. 后置验证

- 已注册 worktree：`54 → 8`；
- 本地 heads：`53 → 13`；
- `git fsck --connectivity-only --no-dangling --no-progress`：通过；
- surviving worktrees 的状态已复核；M9c、ADR-040 和 WP-00a 的未提交内容保持原状；
- 文件系统空闲空间：
  - 清理前：`36,050,840 KiB`；
  - 移除 worktree 后即时：`37,407,044 KiB`；
  - 后置复核时：`36,950,048 KiB`。

> 说明：各 worktree 与共享 Git 对象、依赖缓存之间存在共享/并发写入，`du` 的目录总和不可等同实际释放空间；只以每次 `df` 观测作为当时文件系统空间证据。

## 6. 结论

**PASS（清理范围内）**：历史 worktree 目录和已证明冗余的本地 refs 已清理；当前开发 WIP、审计证据、保留分支与远端分支未被误删。5 个未证明安全的 refs 被明确保留，后续如需继续收敛，必须先为每个 ref 取得合入或远端镜像证据。
