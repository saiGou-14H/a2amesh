# A2AMesh 项目完成进度留档

> 快照时间：2026-08-17 11:56（Asia/Shanghai）
> 项目状态：持续开发中；当前实现仍是 **private A2A-inspired NATS RPC prototype**，不得声明为官方 A2A v1 兼容或生产就绪。  
> 记录类型：阶段性进度快照，不替代最终发布验收报告。

## 1. 结论

按完整目标——`CORE + INTEROP + EXTENDED + Linux/Windows 三机部署 + 生产发布门禁`——按实施计划 G0、C0～C8 加权估算：

- **整体完成度：约 23.7%（对外简化为约 24%）**
- 合理估算区间：**15%～25%**
- 剩余工作量：约 **76%**

这不是按代码行数、文档页数或测试数量计算。纯状态合同测试通过，不等于真实 Redis/NATS/Object Store/Runtime/Windows 部署已经完成。

### 1.1 分口径进度

| 口径 | 估算进度 | 说明 |
|---|---:|---|
| 设计文档与架构治理 | 约 90% | 当前 G0 候选集基本形成；仍需独立复审、冻结和实现追溯闭合 |
| R11-C/R11-D | 约 95% | 实现、本地门禁、二次独立复审和普通 checkpoint 已闭合；非生产验收 |
| 私有可运行原型 | 约 40% | 私有 NATS、Runtime、Adapter、Orchestrator、MCP 等已有基础 |
| 官方 A2A CORE | 约 15%～20% | Canonical Core、Redis State、Gateway、完整Binding尚缺 |
| 完整产品/生产交付 | **约 24%** | 三机部署、故障恢复、安全和发布门禁尚未完成 |

## 2. 加权估算明细

| 阶段 | 总权重 | 当前完成度估算 | 对总体贡献 |
|---|---:|---:|---:|
| G0 设计与治理 | 8% | 90% | 7.2% |
| C0 官方 fixture/CI 基线 | 4% | 80% | 3.2% |
| C1 Canonical A2A Core | 12% | 20% | 2.4% |
| C2 Redis State | 20% | 5% | 1.0% |
| C2.5 控制面垂直切片 | 16% | 15% | 2.4% |
| C3 完整 NATS Binding | 12% | 25% | 3.0% |
| C4 Supervisor/Runtime | 10% | 30% | 3.0% |
| C5 JSON-RPC/SSE Gateway | 8% | 5% | 0.4% |
| C6 INTEROP/EXTENDED | 4% | 20% | 0.8% |
| C7/C8 部署、恢复与发布门禁 | 6% | 5% | 0.3% |
| **合计** | **100%** | — | **23.7%** |

以上百分比是工程管理估算，不是验收结果；每个阶段仍以对应退出门禁为准。

## 3. 已完成或基本完成

### 3.1 设计和治理

已形成并维护：

- 总体业务和部署架构；
- A2A 对象与协议设计；
- NATS Binding 设计；
- Redis 状态平面设计；
- Runtime/Adapter/编排器设计；
- Artifact/Object Store 设计；
- Config、Reconciliation、Audit、Recovery 设计；
- 分阶段实施计划；
- TEST ID 与 current/target 追溯矩阵；
- 架构 SVG/HTML twin 和机器边治理。

### 3.2 R11-C：Artifact Hold authority 合同

当前候选实现已覆盖：

- State-side SCAN candidate allocation；
- allocation provenance/proof；
- SCAN authority seal；
- REPLAY current-authority pointer；
- owner/lease/token/fence/digest/revision 绑定；
- EXPIRE CAS snapshot；
- candidate tombstone、commit、audit、outbox 一致性；
- exact replay 和 fail-closed 负例。

状态：代码、本地门禁、修复后二次独立复审和普通 checkpoint 已完成；仍非真实 Redis/Object Store/生产验收。

### 3.3 R11-D：架构资产治理

已完成：

- 删除未经授权的 Artifact/Reaper → Recovery 路径；
- machine-edge 精确 allowlist；
- 全部 SVG path 集合指纹；
- SVG/HTML twin 一致性；
- Chromium 浏览器资产门禁。

### 3.4 私有原型迁移输入

已有但尚非正式产品合同：

- 私有 Pydantic AgentCard/Task/Message；
- 私有 NATS request/reply、部分 streaming/get/cancel；
- Runtime Executor；
- Hermes/Codex/Claude/OpenCode Adapter；
- Planner/Dispatcher/Tracker/Aggregator；
- Identity/AuthContext/Credential 原语；
- Tool Registry；
- MCP Client 和有限 Server Bridge；
- NATS KV/MemoryStore；
- 官方 `a2a-sdk==1.1.2` facade 初步边界。

## 4. 当前已验证证据

以下是阶段性证据，不代表完整生产验收：

- Artifact Hold 专项：`85 passed`；
- Artifact + 架构资产专项：`94 passed`；
- R11 checkpoint 候选树全量：`329 passed, 7 skipped`；
- R11 + 进度留档 + C0-1 初始树全量：`336 passed, 7 skipped`；
- C0-1修复后的合并树全量：`340 passed, 7 skipped`；
- C0-1 conformance：`15 passed`，官方 fixture verifier：`7/7`；
- C0-1修复后独立复审：`VERDICT=PASS`，原1个P1和3个P2均关闭；
- C0-2c独立复审：`VERDICT=PASS`，P1=NONE；专项 `31 passed`，fresh-venv wheel probe、sdist→wheel资源8/8通过；仅保留Linux/Windows边界与可选import/hash增强P3；
- C0-2b整改后二次独立复审：`VERDICT=PASS`（C0整改范围）；确认原2个P1和2个P2关闭，`ce83492`本地clean报告8/8；远端GitHub Actions未运行，仍非完整A2A/生产验收；
- C0/C1-1提交后clean机器报告：`8/8 core-gates passed`，C1候选全量环境 `370 passed, 8 skipped`；
- C0真实Chromium：`1 passed`；自动sdist closure：250文件、解包conformance `36 passed`；
- C1-1初始checkpoint：`7c793bf`，tree `8f13139`；可信Principal Context、首个GetTask unary contract、official error facade、NATS unary接线已实现；独立复审发现P1/P2并判`BLOCKED`；remediation checkpoint `6baef3d`，tree `7f788e1`，待独立复审；
- C1-2 checkpoint：`f7bb70a`，tree `ff4e9a1`；官方TaskState迁移表/终态保护、facade导出、NATS重复终态集合清理；提交后`8/8` gates、`389 passed, 8 skipped`、sdist closure `253 files/36 passed`、wheel资源`8/8`；独立复审`VERDICT=PASS`（P1/P2=NONE，P3仅当前规范与旧归档图版本注意）；仍只是纯状态合同；
- wheel无依赖、非 editable 安装资源门禁：manifest+7类fixture共`8/8`资源可读；
- Ruff、compileall、diff check：通过；
- sdist 隔离专项：通过；
- wheel 非 editable 安装/import smoke：通过；
- 开放 NATS transport smoke：`1 passed`；
- secure NATS ACL smoke：`6 passed`。

真实 NATS smoke 使用临时 broker；临时端口、容器和认证资料已清理。smoke 不等于完整 89-subject ACL、Stream Session、durable Event Relay 或生产 TLS/account 验收。

## 5. 待实现工作模块

### 5.1 C0：基线冻结与官方协议准备

- [x] 完成官方 Card/Message/Task/Artifact/Event/Error fixture 闭包；
- [x] 严格 fixture verifier、sdist收录与隔离venv门禁；
- [x] A2A Spec v1.0.1 与 SDK 1.1.2 固定；
- [x] TaskNotFound error fixture 与 SDK `build_error_response` wire shape 严格绑定；
- [x] Message role/messageId/text oneof、manifest symlink、duplicate-key负例回归；
- [x] 旧私有 fixture 与官方 fixture 通过manifest和目录边界明确隔离；
- [x] 机器可读 CI 报告和固定开发依赖；
- [x] README/current-target兼容声明可执行门禁；
- [x] 活动Markdown本地链接/anchor检查；
- [x] wheel 内官方 fixture 资源布局与读取门禁。

### 5.2 C1：Canonical A2A Application Core

- [x] 官方对象 re-export 和首个适配边界（C1-1）；
- [x] `CanonicalRequestContext` trusted Principal contract 与首个 `GetTask` unary dispatch contract；
- [ ] 11 个 transport-independent async 操作；
- [x] 官方 TaskState 合法迁移和终态保护（C1-2纯协议合同；C2 version/lease/fencing/CAS仍未实现）；
- [ ] Card builder、validator、ETag；
- [ ] Runtime/Progress 扩展；
- [ ] 统一错误分类；
- [x] 官方 tenant 非空前置校验（C1-1 Core validator；全11操作覆盖仍待完整contract套件）；
- [ ] 每个操作的 transport-independent contract test；
- [ ] 禁止新代码导入 legacy Pydantic 标准对象。

### 5.3 C2：真实 Redis State Service

- [ ] Redis async client/repository；
- [ ] mesh_id 安全 key builder；
- [ ] `claim_auth_request`、`claim_message` Lua/Function；
- [ ] Task/Card/Principal/Credential/Alias 权威状态；
- [ ] messageId 幂等、lease、fencing、replay；
- [ ] outbox、dispatch、effect ledger；
- [ ] admission/DRR、Plan、workspace；
- [ ] Artifact/config/reconciliation/audit/recovery 状态；
- [ ] Get/List/cursor；
- [ ] Redis restart、双实例和故障注入。

### 5.4 C2.5：控制面垂直切片

- [ ] Artifact/Object Store upload/finalize/download/delete/orphan；
- [ ] Config signed bundle、stage/activate、rollout/recovery；
- [ ] Reconciliation UNKNOWN/Evidence/claim/resolve；
- [ ] append-only Audit Sink、Segment JWS、WORM receipt；
- [ ] Recovery Manifest、summary DAG、restore/verification receipt；
- [ ] Redis/Object Store/NATS/config/audit 共同恢复判定。

### 5.5 C3：完整 NATS Binding

- [ ] 11 操作完整 subject 映射；
- [ ] literal State subject ACL 全矩阵；
- [ ] AuthProof handler；
- [ ] private inbox；
- [ ] durable Dispatch Worker 和 Event Relay；
- [ ] JetStream PubAck/outbox head；
- [ ] Stream Session Controller、JS Provisioner、operation ticket；
- [ ] broker restart/reclaim/expiry；
- [ ] TLS/account/NKey 生产配置。

### 5.6 C4：Supervisor/Runtime

- [ ] durable heartbeat、runtime lease/fencing；
- [ ] process authority、deadline、cancel race；
- [ ] crash takeover；
- [ ] side-effect intent/attempt/UNKNOWN ledger；
- [ ] Linux/Windows process containment；
- [ ] workspace fencing；
- [ ] 固定版本 Adapter 真机门禁。

### 5.7 C5：Gateway 和官方黑盒

- [ ] `/.well-known/agent-card.json`；
- [ ] JSON-RPC、HTTP/SSE、Bearer、ETag；
- [ ] A2A 扩展头和统一错误映射；
- [ ] 官方客户端黑盒；
- [ ] CORE profile 验收；
- [ ] Gateway 不得绕过 Canonical Core/State。

### 5.8 C6：INTEROP/EXTENDED

- [ ] 官方 gRPC Binding；
- [ ] Push Dispatcher、签名、SSRF、retry/DLQ；
- [ ] 额外 Runtime Adapter；
- [ ] MCP 完整 Bridge；
- [ ] OAuth AS/JWKS；
- [ ] Observer 和受控干预；
- [ ] 官方 gRPC/MCP/OAuth 黑盒。

### 5.9 C7/C8：三机、故障和发布

- [ ] 公网 Linux 注册中心；
- [ ] 两台不同 NAT 后的原生 Windows Peer；
- [ ] 出网注册和对称调度；
- [ ] 网络分区、NATS/Redis/Object Store 重启；
- [ ] Windows 进程崩溃和恢复；
- [ ] Linux + 1 NAT CORE 门禁；
- [ ] Linux + 2 Windows INTEROP/EXTENDED 门禁；
- [ ] metrics/tracing/alert；
- [ ] backup/restore、RTO/RPO；
- [ ] 容量、延迟、安全、发布、升级、回滚门禁。

## 6. 明确不属于 V1 的内容

以下是V1有意排除，不计入“未完成缺陷”：

- tenant/多租户；
- RBAC 和 Permission Center；
- 通用后台 UI；
- Redis Cluster/跨区域 HA；
- 任意 shell 公开 Skill；
- UNKNOWN 副作用自动重试；
- 原始 Chain-of-Thought；
- 多 Mesh 联邦。

## 7. 模块化执行规则

每个后续模块必须独立完成以下闭环：

1. 读取对应设计章节和 TEST ID；
2. 明确 current/target/authority/failure/acceptance；
3. 先写 RED 测试；
4. 实现最小可验证增量；
5. 删除已被替代且无调用者的旧代码；
6. 保留仍有调用者的兼容代码，并标记 `legacy/compatibility-only`；
7. 运行专项、静态、隔离包和必要真实依赖门禁；
8. 独立只读复审；
9. 创建可回滚普通 checkpoint commit；
10. 只有最终复审和全量门禁通过后，才允许使用 verified 语义；
11. 未经明确授权不 push。

## 8. 当前阶段阻断

- C0-2c wheel资源独立复审已 `VERDICT=PASS`（P1=NONE）；仅保留当前Linux执行、Windows symlink skip和可选import/hash增强等边界，不把它写成完整A2A/生产验收；
- C0-2b原复审 `VERDICT=BLOCKED` 的2个P1和2个P2已由 `ce83492`本地整改并经二次独立复审 `PASS`；仅为C0整改范围，未取得远端Actions证据；
- C1-1初始 `7c793bf`及第一轮remediation `6baef3d`经独立复审为 `BLOCKED`；第二代`0731560`追加对抗探针仍发现外层policy map、可变str/credential、hostile hash、retryable和expected-generation边界缺口；第三代修复工作树待复审，不标记 `verified`；
- C1-2 `f7bb70a`已合入main并通过提交后8/8机器门禁，独立复审 `VERDICT=PASS`（P1/P2=NONE，P3仅版本文档注意）；仅为纯TaskState合同，不代表C2 State或完整C1；
- GitHub workflow已落盘但尚未push，因而没有远端GitHub Actions和阿里云锁源可达性证据；
- R11 已通过短独立只读复审并创建普通 checkpoint，但仍只证明纯Python状态合同和架构资产门禁；
- 所有本轮commit均尚未 push；
- 当前仍不能宣称官方 A2A v1 兼容、完整 CORE、三机互操作或生产就绪。

## 9. 更新记录

| 时间 | 更新 |
|---|---|
| 2026-08-17 08:05 Asia/Shanghai | 首次记录整体约20.5%估算、R11状态、C0～C8剩余工作和模块化执行规则 |
| 2026-08-17 08:19 Asia/Shanghai | R11独立复审 `VERDICT=PASS`；创建 `b60b821`；合入进度文档 `de34fa0` 与 C0-1 `03ccdd8`；合并树 `336 passed, 7 skipped`；C0-2进入RED |
| 2026-08-17 10:29 Asia/Shanghai | C0-1独立复审发现1个P1和3个P2；`9d93c6a`绑定SDK TaskNotFound wire并补齐语义/路径/duplicate-key负例；合并树 `340 passed, 7 skipped`；C0-2a `f3c407d`待合入 |
| 2026-08-17 10:44 Asia/Shanghai | 合入C0-2a `e3d8ee4`和C0-2b `da4fac3`；main clean机器报告6/6、`352 passed, 8 skipped`；Chromium `1 passed`；sdist conformance `28 passed`；C0-2c待完成 |
| 2026-08-17 10:48 Asia/Shanghai | C0-1修复后独立复审 `VERDICT=PASS`；确认TaskNotFound SDK wire及Message/symlink/duplicate-key原P1/P2全部关闭；能力边界仍非完整A2A或生产验收 |
| 2026-08-17 11:06 Asia/Shanghai | 合入C0-2c `ab9002d`；机器报告7/7、`357 passed, 8 skipped`、wheel资源8/8、sdist conformance 33；C0-2b独立复审BLOCKED，2个P1和2个P2进入整改 |
| 2026-08-17 11:16 Asia/Shanghai | 合入C0整改 `ce83492`；精确pin和浏览器命令统一；机器CI新增真实sdist closure及报告异常/scope合同；clean core报告8/8、`360 passed, 8 skipped`；二次复审待返回 |
| 2026-08-17 11:34 Asia/Shanghai | C0-2c独立复审 `VERDICT=PASS`（P1=NONE；专项31 passed；fresh-venv wheel/sdist资源8/8）；C1-1 `7c793bf`/tree `8f13139`合入main；提交后机器报告8/8、全量候选370 passed/8 skipped；C1-1二次复审、C0-2b二次复审待返回 |
| 2026-08-17 11:56 Asia/Shanghai | C1-2 `f7bb70a`/tree `ff4e9a1`合入main；官方TaskState纯合同提交后8/8、389 passed/8 skipped、sdist closure 253/36、wheel 8/8；C1-2独立复审、C1-1复审、C0-2b二次复审待返回 |
| 2026-08-17 12:16 Asia/Shanghai | C0-2b整改后二次独立复审 `PASS`（C0范围）；C1-1初始复审 `BLOCKED` 的P1/P2在独立checkpoint `6baef3d`修复；修复树`7f788e1`本地`8/8`、`398 passed/8 skipped`、sdist `253/36`、wheel `8/8`，等待remediation独立复审 |
| 2026-08-17 12:44 Asia/Shanghai | C1-1第一轮remediation独立复审仍为`BLOCKED`：发现SignerPolicy mutable set扩权、未知/爆炸A2A异常错误映射可能泄漏/丢回复、client/server/request/legacy boolean generation缺口；第二代remediation工作树补齐冻结集合、固定错误字段与total fallback、全generation exact-int回归，待新的同树独立复审 |
| 2026-08-17 13:16 Asia/Shanghai | 对第二代`0731560`追加对抗探针发现外层policy map可替换、mutable str/credential浅层alias、hostile hash、retryable integer、verifier expected-generation bool和AuthContext类混淆；第三代fix3补齐exact plain primitives、只读policy snapshots、exact error/generation/type gates，hardening专项14 passed，待提交后门禁与同树复审 |

## 10. 当前可回滚模块索引

| 模块 | Commit | 状态 |
|---|---|---|
| R11-C/R11-D authority与架构资产 | `b60b821` | 普通checkpoint；独立短复审PASS；非生产验收 |
| 项目完成进度留档 | `de34fa0` | 已合入main；待后续阶段追加更新 |
| C0-1官方A2A fixture基线 | `03ccdd8` + `9d93c6a` | 初始checkpoint被复审打回；修复后独立复审PASS；非完整兼容/生产验收 |
| C0-2a文档链接与兼容声明 | `e3d8ee4` | 已合入main；活动文档0 finding；7项专项通过 |
| C0-2b CI/依赖/机器报告 | `da4fac3` + `ce83492` | 初审BLOCKED；整改已合入并通过本地8/8；二次独立复审PASS（C0整改范围）；无远端Actions证据 |
| C0-2c wheel fixture资源 | `ab9002d` | 已合入main；clean报告7/7；独立复审PASS；仍非完整兼容/生产验收 |
| C1-1 trusted Context + GetTask unary contract | `7c793bf` | 已合入main；提交后8/8机器门禁；二次独立复审待返回；C1整体未完成 |
| C1-1 review remediation：服务端Principal provenance/错误闭合 | `6baef3d` + 第三代待提交 | 第一轮remediation复审BLOCKED；第二代追加探针仍BLOCKED；第三代fix3未提交/未复审；不代表完整A2A/生产验收 |
| C1-2 official TaskState transition contract | `f7bb70a` | 已合入main；提交后8/8机器门禁；独立复审待返回；不含Redis/CAS/生产验收 |

后续每完成一个可回滚模块，应在本文件追加一行更新，并同时保留对应commit、测试命令和真实输出引用；易漂移的测试通过数量以机器生成CI报告为准，不手工伪造固定数字。
