# A2AMesh 执行进度日志（2026-08-21）

> 日志 ID：`A2AM-EXEC-LOG-20260821`
>
> 排期：[`A2AMesh_未完成项与交付排期_2026-08-21.md`](A2AMesh_未完成项与交付排期_2026-08-21.md)
>
> 日志规则：仅记录可复核的 commit/tree、命令结果、外部运行 ID、明确阻断和已完成的退出条件。候选、局部测试、本地 replay 或无结论的审阅均不得升级为阶段完成或发布通过。
>
> 推送规则：状态发生变化时自动推送；无变化时保持静默。计划轮询上限为 5 分钟，不能替代实时外部 CI webhook。

---

## 2026-08-21（Asia/Shanghai）

### 11:56 — E-001：排期 checkpoint 已远端交付

- 排期 commit：`63fd0587e8e98fd70f472d35b87d2ec1381b3871`
- 排期 tree：`aebbd4acf5afcfaa6a0d1cb0dda631dc252253ab`
- 远端分支：`origin/docs/project-execution-schedule-2026-08-21`
- 远端 SHA 回读：与上述 commit 一致。
- 提交范围：仅排期 MD；排期 worktree 在 push 后为 clean。
- 该 checkpoint 曾完成精确 tree 独立复审 `VERDICT=PASS; P1=0; P2=0`，且本地 disposable Redis + JetStream core gate 为 8/8 PASS。
- 边界：本地证据不等同 GitHub-hosted CI 或任何 A2AMesh 功能/发布阶段通过。

### 11:56 — E-002：Hosted CI #32 失败，WP-00a 开始

- 精确 SHA：`63fd0587e8e98fd70f472d35b87d2ec1381b3871`
- GitHub Actions：`A2AMesh CI #32`，event=`push`。
- `chromium-gate`：`success`。
- `core-gates`：`failure`；失败步骤为 `Run machine-readable gates`（exit code 1）。
- GitHub 公开页面/API 未给出具体 gate 输出；Artifact 下载在当前无 API 授权的执行环境返回未授权。因此根因状态为 **`UNDETERMINED`**，不得归因于本次文档、Redis、NATS、依赖或测试中的任一项。
- 当前工作包：**WP-00a Hosted CI #32 失败取证与归因**。
- WP-00a 的下一证据：取得授权 report/log，或者在隔离精确 worktree 以等价环境复现，定位具体失败 gate 与最小复现命令。

### 11:56 — E-003：Wave 0 其他阻断快照

- 功能基线：`8100b9312e7432e2b863f2ccf09ff89cf361d687` / tree `988f49d47a9326acdd6b7afb5532622d27eb8996`，审计 worktree clean；功能分支未由远端广告。
- M9c：`work/c2-ci-nats-step-exec-m9c`，`tests/test_redis_ci_contract.py` 未提交修改（94 insertions / 5 deletions）；cleanup failure contract 尚未闭环。
- ADR-040：`work/c2-claim-message-a1-adr` 中仍为 Proposed/未提交，且 ADR 索引有未提交修改。
- G0：仍未批准。
- 因此 A1 `claim_message` 和后续 C2～C8 实现保持 blocked；不得发布 CORE / INTEROP / EXTENDED 兼容或生产就绪声明。

### 12:18 — E-006：WP-00a hosted diagnostic 候选已完成本地 checkpoint，等待独立复审

- 候选分支：`work/ci-hosted-diagnostic-wp00a`；base：`63fd0587e8e98fd70f472d35b87d2ec1381b3871`。
- 本地 checkpoint：`83c7633431f1f2cf77e9647e1099ff33008bbcfb`；tree：`26067b82814eaaef3d13e54cf4e8df8f258900f2`；仅修改 `.github/workflows/ci.yml` 与 `tests/test_redis_ci_contract.py`。
- TDD：新 contract 曾预期 RED，之后 GREEN（文件级 `4 passed`）；PyYAML parse、提取的 workflow failure step、`actionlint 1.7.12` 均通过。
- 安全边界：在 `Run machine-readable gates` 失败后、NATS cleanup 前，诊断步骤只输出 failed gate ID 与 `returnCode` 的 GitHub annotation；不输出 `stdout`、`stderr`、report 原文或环境值，也不会改变原失败 exit status。
- post-commit 本地 disposable Redis+NATS：`run_ci.py` 为 `8/8 gates PASS`，pytest `664 passed, 7 skipped`；报告：`/tmp/a2amesh-wp00a-postcommit-20260821041813-698707-ci-report.json`；容器由 cleanup trap 清理，worktree clean。
- hosted 状态：**尚未推送，尚未产生该候选的 hosted run**。排期分支的纯文档 SHA `63fd…`、`5fd…`、`1be…` 均为 `core-gates` failure；这支持优先诊断，但不构成根因结论。
- 当前结论：`PENDING_INDEPENDENT_REVIEW`；独立审阅明确 `VERDICT=PASS` 后才允许 push 触发诊断性 hosted CI。

### 12:22 — E-007：WP-00a 精确候选经独立复审拒绝，进入 hardening RED

- 审阅对象仍为 `83c7633431f1f2cf77e9647e1099ff33008bbcfb` / `26067b82814eaaef3d13e54cf4e8df8f258900f2`；三份独立只读审阅均为 `VERDICT=FAIL`，不得推送。
- 共同 P1：不可信 `.ci-artifacts/ci-report.json` 的 `id` / `returnCode` 未验证类型或 CR/LF，存在 GitHub workflow-command 注入和原始值泄露面；非 UTF-8 或畸形 report 也可能令诊断脚本异常退出。
- P2：`failure()` 触发范围过宽，不限于 `Run machine-readable gates`；旧 contract 偏向字符串检查，不能充分阻止 `print(report)` 类回归。
- 已新增实际执行 workflow summary shell 的 hardening tests，并得到预期 RED；尚未形成新的 GREEN candidate。
- 边界：`83c7633…` 保留为本地被拒绝 checkpoint；未 push、无该候选 hosted run，原 hosted failure 根因仍为 `UNDETERMINED`。

### 12:03 — E-005：WP-00a 的本地等价复现通过，但 hosted 根因仍未确定

- 审计对象：精确失败提交 `63fd0587e8e98fd70f472d35b87d2ec1381b3871`，tree `aebbd4acf5afcfaa6a0d1cb0dda631dc252253ab`。
- 在临时 detached worktree 中使用锁定依赖执行：`uv sync --locked --extra test --extra state`，随后以 workflow 所用 Redis/NATS image digest 启动隔离 broker，并执行 `uv run --locked --extra test --extra state python scripts/run_ci.py`。
- 结果：`REPORT_STATUS=passed`，8/8 gate 均为 rc=0（official fixtures、docs links、sdist closure、wheel resources、pytest、ruff、compileall、git diff check）。报告 SHA-256：`4e6dbc7088891972987ce17ce0794cd15475180b810b17746252e500f775b09a`。
- 清理：临时 worktree 已删除、临时 Redis/NATS 容器数量为 0；复现树在执行后为 clean。
- 结论边界：这排除了“在该隔离等价本地环境中可稳定复现失败”；它**不能**推翻 GitHub-hosted 的 failure，也不能证明其为平台偶发问题。hosted 具体失败 gate 仍需要授权 report/log 才能归因。
- WP-00a 状态改为 **`BLOCKED_AUTH_REPORT`**；在取得报告或可审计的 hosted diagnostic run 前，不进入 WP-01 的代码修改阶段。

### 12:00 — E-004：进度变更推送已启动

- 监控任务：`A2AMesh 执行进度变更推送`，每 5 分钟轮询一次。
- 发送策略：仅在排期、功能基线、M9c、ADR-040 worktree 状态、远端排期 SHA 或本执行日志发生变化时推送；无变化保持静默。
- 首次执行已成功；脚本不读取或输出 token、连接串、seed 等机密。
- 边界：这是轮询式变更通知，最长约 5 分钟延迟；不能替代 GitHub webhook 或实时终端流。

### 15:43 — E-008：历史 worktree 与本地 ref 清理已完成

- 审计文档：[`A2AMesh_历史工作树与分支清理审计_2026-08-21.md`](A2AMesh_历史工作树与分支清理审计_2026-08-21.md)。
- 在清理前冻结 `54` 个注册 worktree、`53` 个本地 heads；逐个使用 `git worktree remove` 移除 `46` 个 clean、无进程占用的历史目录，未使用 `--force`。
- 随后仅按可证明条件删除 `40` 个本地 refs：`39` 个已被保留分支包含，`1` 个是 `origin` 上同 SHA 的镜像；`5` 个未证明安全 refs 显式保留。
- 未删除 remote branch、未删除任何 dirty WIP、未运行 `git gc`；detached ADR-039 审阅 HEAD 已确认被保留分支包含。
- 后置验证：注册 worktree `54 → 8`、本地 heads `53 → 13`，`git fsck --connectivity-only --no-dangling --no-progress` 通过；M9c、ADR-040、WP-00a 的未提交状态保持原状。

### 15:59 — E-009：保留项目工作树已收拢至统一开发根目录

- 目录迁移审计：[`A2AMesh_工作树目录迁移审计_2026-08-21.md`](A2AMesh_工作树目录迁移审计_2026-08-21.md)。
- 新根目录：`/root/project-development/A2AMesh/`；主仓为 `main/`，其余 `7` 个 linked worktree 位于 `worktrees/`。
- 迁移前后：`8` 个注册 worktree 均保持相同 HEAD、branch、status；6 个未提交 WIP 文件的 SHA-256 全部一致；`git fsck --connectivity-only --no-dangling --no-progress` 通过。
- Git `2.34.1` 无 `worktree repair`，因此对当前无 submodule/lock 的布局先做 disposable 迁移验证；7 个 linked tree 经 `git worktree move`（dirty tree 仅用 `--force` 保全 WIP）迁移，主仓 `rename` 后逐项修复并验证 linked `.git` admin 指针。
- 进度 cron job 在迁移窗口暂停、路径更新与 `py_compile`/导入验证后恢复；旧 8 个活跃 worktree 路径已不存在。
- 边界：仅为本机目录和监控配置迁移；未改变 commit/tree、远端、hosted CI、功能阶段或发布结论。

---

## 活跃工作包

| 优先级 | ID | 状态 | 进入条件 | 当前下一步 | 退出条件 |
|---:|---|---|---|---|---|
| P0 | WP-00a | `BLOCKED_HARDENING` | checkpoint `83c7633…` / `26067b…` 的三份独立 review 均 FAIL；新 hardening test 已 RED | 在隔离 worktree 完成 report UTF-8/JSON/schema/CRLF 校验、精确 gate failure condition 与行为型防泄漏测试 | 新 exact candidate 的 local gates + 独立 review 明确 PASS 后，才允许 push 并观察 hosted 安全摘要。 |
| P0 | WP-01 | `BLOCKED` | WP-00a 未结论；M9c WIP 存在 | cleanup failure RED→GREEN | 三分支 cleanup 语义、最小 workflow 修复、targeted/file gates。 |
| P0 | WP-02 | `BLOCKED` | WP-01 | exact candidate/review/push/hosted CI | local + review + hosted exact SHA 均 PASS。 |
| P0 | WP-03 | `BLOCKED` | G0 当前未批准 | R10 final tree 与 A/B/C review | manifest、P0/P1=0、正式批准。 |
| P0 | WP-04 | `BLOCKED` | WP-02、WP-03 | integration tree | remote-tracked clean tree 与 exact evidence。 |

> 下一次日志只记录状态、证据、commit/tree、明确阻断或 gate 变化；不会打印凭据、连接串、token、seed 或其他机密。
